- use GNU error instead of fprintf
- wolp (Wake On LAN proxy) support http://etherboot.sf.net/wolp, maybe a
  reimplementation
- resolv names
- catch argument cases where no MAC-ADDRESS is given but eg -v
- fix it and fr translation
- wol.gmo in hpux isn't here? bug in gettext?
- magic packets > 1 -> put several packets into one buffer for sending over the
	line
- ipv6? only for interest, cause there is no ipv6-only host out there, huh?
- a graphical frontend with some cool gimmicks?
